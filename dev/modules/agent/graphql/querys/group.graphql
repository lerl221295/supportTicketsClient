#import "../fragments/groupsFragment.graphql"
query GetGroup($id : ID!) {
	group(id: $id) {
        ...groupsFields
        group_scale {
            id
            name
        }
        agents {
            id
            name
        }
        notification_agent {
            id
            name
        }
        notification_hours
        notification_text
	}
}