#import "../fragments/ticketsListFragment.graphql"
query GetTickets($filter: FilterTicketInput, $offset: Int, $limit: Int) {
    tickets(filter: $filter, offset: $offset, limit: $limit){
        nodes {
            ...ticketsListFields
        }
    }
}